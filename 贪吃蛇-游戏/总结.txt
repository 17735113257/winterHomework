体验面向对象的编程思维

明确需求：
找对象
构造函数 创建对象 根据需求明确对象的属性和方法 
属性一般是变化的  
属性也可以定义其他对象
方法：对象能干什么      


this关键字:
    1：构造函数中指向实例  当前创建的对象
    2：定时器默认指向window
    3：普通方法中指向调用者  点前面的
    4：原型方法中 指向实例
返回值 return：
    1：若函数内部要向外暴露数据时候 使用返回值
    2：若返回多个数据时候  可以用数组字面量或是是对象字面量
    3：若返回肯定  否定数据时候  返回布尔值
需要外部传入内容时候用参数